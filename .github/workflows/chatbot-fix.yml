name: Legacy chatbot fixer (disabled)
on:
  workflow_dispatch:
jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled; FPS ARENA uses the smart chatbot installer."
