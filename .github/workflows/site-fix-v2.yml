name: Legacy FPS ARENA site fixer (disabled)
on:
  workflow_dispatch:
jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled; FPS ARENA now uses the stable smart chatbot workflow."
