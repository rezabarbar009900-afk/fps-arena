name: Legacy AI upgrader (disabled)
on:
  workflow_dispatch:
jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled; FPS ARENA uses the current real AI."
